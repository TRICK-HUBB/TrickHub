local PlaceScripts = {
    [120249927456668] = "https://raw.githubusercontent.com/TRICK-HUBB/TrickHub/refs/heads/main/DeltaRhythm",
    [6403373529] = "https://raw.githubusercontent.com/TRICK-HUBB/TrickHub/refs/heads/main/Get%20All%20Gloves"
}

local placeId = game.PlaceId
local scriptUrl = PlaceScripts[placeId]

if scriptUrl then
    local success, err = pcall(function()
        loadstring(game:HttpGet(scriptUrl))()
    end)
  if not success then
warn("error")
end
else
   warn("unsupported Place")
end
